# 이름 짓기
name: Github Actions 연습하기
# 언제 실행할꺼야?
on: push

# 뭘 실행할꺼야? -> 직업 부여
jobs:
  push-job:
    # 컴퓨터 어떤 환경에서 실행할꺼야?
    runs-on: ubuntu-latest
    # 일의 순서
    steps:
      - name: 첫 번째 작업
        run: echo "첫 번째 작업 실행 중"
